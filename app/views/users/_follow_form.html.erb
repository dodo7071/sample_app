<% unless current_user?(@user) %>
	<div id="follow_form">
		<% if current_user.following?(@user,current_user) %>
			<%= render 'unfollow' %>
		<% else %>
			<%= render 'follow' %>
		<% end %>
	</div>
<% end %>
